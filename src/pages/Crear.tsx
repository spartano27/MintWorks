import React from 'react';  
import {CreateRoom} from "../components/rooms/createRoom";

/**
 * Crear() returns a CreateRoom component.
 * @returns A component called CreateRoom.
 */

function Crear(){

    return (
        <CreateRoom/>
    );
}

export default Crear;
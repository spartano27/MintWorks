export enum ShopCardsTypes {
    shop = "shop",
    neighborhood = "neighborhood",
    active = "active"
}

export enum CardTypes {
    culture = "culture",
    production = "production",
    utility = "utility",
    deed = "deed"
}
